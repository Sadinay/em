# 遗传算法区

该区域负责新200位染色体的编码、初始化、选择、交叉、变异、约束、精英保留和停止条件。

- `scripts/`：GA核心模块和终端入口；
- `configs/`：种群规模、交叉率、变异率、目标函数和约束配置；
- `checkpoints/`：完整随机状态和可恢复断点；
- `runs/`：每代种群、适应度、谱系、失败个体和运行历史。

目前只确认MAT中记录了`pcrossover=0.5`和`pmutation=0.1`，尚未确认其具体算子行为。

