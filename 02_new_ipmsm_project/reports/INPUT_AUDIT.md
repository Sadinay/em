# 新项目输入初步审计

审计日期：2026-08-19

## 文件清单

| 文件 | 大小 | 格式 | SHA-256 |
|---|---:|---|---|
| `femm_zone/models/IPMSM.fem` | 111,945字节 | FEMM 4.0文本模型 | `696e68c9f0cbedc7c82ba621f10496fef031b292ff69d4c969b4642ebb515e82` |
| `data_zone/raw/workspace_600.mat` | 23,954,208字节 | MATLAB 5.0 MAT | `26b1dc59b72c4b856b75f9a92358d60d1b26093c9f634a44426df98a82271193` |

## 已确认事项

- 新染色体记录长度为200，而不是旧项目的180；
- 当前种群为504×200的二进制矩阵；
- 解码后存在504×100的材料矩阵，材料编号观察到0、1、2、3；
- 保存了600代最佳适应度和601个状态截面的全种群历史；
- 保存了平均转矩、DeltaT、适应度、永磁体体积和材料连通性等历史量；
- MAT 中记录 `pcrossover=0.5`、`pmutation=0.1`；
- 新 FEM 文件包含与旧模型不同的几何、材料和边界定义。

## 尚未确认事项

- 200位二进制染色体如何成对或分组解码为100个0～3材料值；
- 100个材料位置与 `MaterialPosition[100,8]` 各列的准确几何含义；
- 504种群规模的来源；
- 选择、交叉、变异、精英和终止条件的精确实现；
- `Fitvalue` 的准确公式及优化方向；
- `DeltaT` 是绝对峰峰值、相对脉动率还是其他定义；
- `Tavg` 使用多少个转子角度、角度范围和步长；
- `IPMSM.fem` 与每个染色体之间的材料赋值和几何修改方式；
- MATLAB工作区中保存的函数句柄能否替代缺失的 `.m` 源文件；
- 历史数据中异常小或未初始化值应如何识别。

## 下一步需要的材料

若要完整复现新项目，优先需要与该 MAT/FEM 配套的 MATLAB 主程序和被调用函数，尤其是：

- 种群初始化与材料编码/解码；
- 适应度计算；
- 选择、交叉和变异；
- FEMM模型修改与求解；
- 平均转矩、DeltaT和约束指标计算；
- workspace保存逻辑。

在这些调用链确认前，不应直接套用旧项目的 Python GA 或 FEMM runner。
