# V2 complete three-seed plots

All six models have completed three seeds. Every panel uses the arithmetic mean of test predictions produced by the three best-validation checkpoints. The historical `interim_` filenames are retained so existing links continue to work.
