# 03 pilot20 跨设备 FEMM 核验（2026-09-09）

本次抽查通过：另一台设备的测试使用了此前 G2～G5 成功复现 MAT 参考结果时的物理配置。本机实际重新求解两个基因、每个六个角度，12 个转矩值与测试包逐点完全一致。没有发现本次测试的配置或结果错误。

## 实际重算结果

在重算之前，按测试包中的“平均转矩最高”和“峰峰转矩波动最大”选取两个不同基因。使用本机已有的 `femm_config.py`、`spmsm_mapping.py`、原始 MAT 和 FEM 模板独立生成输入，通过 Windows COM 单进程顺序执行 12 次 FEMM 求解，没有执行 ZIP 中的 Python 代码，也没有复用这两个基因的本地求解缓存。求解总耗时约 60.34 秒。

所有数值单位均为 N·m；转矩波动 ΔT = 六点最大值 − 六点最小值。

| 基因 ID 前缀 | 来源 / 永磁体格数 | 对方平均转矩 | 本机平均转矩 | 对方 ΔT | 本机 ΔT |
|---|---|---:|---:|---:|---:|
| b31089763d90 | U / 120 | 4.423606910642 | 4.423606910642 | 0.839858464542 | 0.839858464542 |
| 05389c43608d | L / 67 | 2.513741122870 | 2.513741122870 | 1.808974291087 | 1.808974291087 |

- 12 个角度的转矩最大绝对差：**0 N·m**。
- 第二个基因的平均转矩差：4.440892098500626e-16 N·m，仅为求和/求平均的浮点舍入差；两个基因的 ΔT 差均为 0。
- 本机求解后的 12 份 FEM、12 份 ANS 的 SHA-256 **全部与对方结果记录相同**。
- 预先采用的核验绝对容差为 1e-6 N·m。

完整 ID：

```text
b31089763d9003ea03c8955b45acbf4d41d7316c1c0cfdd6847e05b5901cad73
05389c43608d9c9aa9c06f61fec2fc929402bc9b0f2de7d9f81adcdb8f106d27
```

![两台设备转矩曲线对比](<torque_comparison.png>)

## 核对的 FEMM 设置

| 项目 | 本次测试、独立重算与历史成功配置 |
|---|---|
| 转子相对初始位置的机械转角 θ | 0°、3°、6°、9°、12°、15° |
| sliding_airgap 内角 | 29°、32°、35°、38°、41°、44° |
| sliding_airgap 外角 | 固定 0° |
| 气隙边界 | Periodic Air Gap，BdryType=6 |
| 三相电流峰值 | 3.5 A |
| 电流电角度 | 从 0°开始，θe=4θ；每步增加 12° |
| 初始三相电流 | Ia=3.5 A，Ib≈−1.75 A，Ic≈−1.75 A |
| 29°初始机械内角 | 仅加到内角，不加到电流初始相位 |
| FEMM 问题 | Planar，millimeters，Frequency=0，Depth=36 mm |
| 网格与精度 | Min Angle=15°，SmartMesh=1，Precision=1e-8 |
| 转矩读取 | mo_gapintegral('sliding_airgap',0)，倍率为 1 |
| 平均值与波动 | 六点算术平均；六点峰峰差 N·m |

电流具体为 Ia=3.5cos(4θ)，Ib=3.5cos(4θ−120°)，Ic=3.5cos(4θ−240°)，此处三角函数角度按度表达。代码通过 MAT 的电角速度与机械角速度及时间计算，二者比值为 4。

## 整包检查与历史依据

1. ZIP 共 431 个文件，CRC 检查通过；`checksums.sha256` 中的 429 个文件哈希全部通过。校验表自身及 `TRANSFER_README.txt` 不在校验表内。
2. 7 个冻结种子/队列 CSV 与本机对应文件逐字节相同，成功结果集合恰为 pilot20 的 20 个基因。
3. 对全部 20 个基因、120 个角度重新生成求解前 FEM 内容，**120 个输入哈希全部与对方记录相同**。同时验证材料映射、基因哈希、永磁体格数、电流、角度、结果与清理凭据的相互关联；六点汇总均正确。
4. 原 MAT、FEM 模板、材料映射源码、当前物理配置源码、FEMM 可执行文件的哈希均与对方一致。
5. 读取此前成功的 G2～G5 历史结果，验证 24 个角度的 48 份 FEM/ANS 文件哈希及 FEM 设置。历史与本次的数值物理配置一致；历史均值/峰峰差与 MAT 参考值的最大差约 1.25e-12 N·m。此项是对已有成功基准的只读核验，没有额外重算历史基因。

对方运行器相对于本机版本的改动是：先导入 Windows COM 并显式选择 COM 通信；成功后保存带哈希的清理凭据、清理 FEM/ANS 并允许凭据校验后的缓存恢复；运行指纹随之更新。差异中没有改变材料映射、电流公式、内外角、问题定义或转矩读取方式。对方报告记载的早期 `NameError: ifile` 属于通信分支问题，最终测试包采用的是修复后的版本；本次重算也明确使用 COM 通信。

## 范围和文件

本次**实际重算两个基因**，并核查全部 20 个基因的记录与输入哈希；没有重新求解其余 18 个基因。ZIP 已不含对方原始 FEM/ANS，因此对方求解文件依据其保存哈希核对，而不是直接读取其文件。本机新生成的 12 组 FEM/ANS 保存在 `rerun/`，可直接审查。

- `check.py`：本次整包检查与两基因求解脚本。默认只审计，`--solve` 才启动 FEMM；已有重算模型时拒绝覆盖。
- `archive_audit.json` / `angle_audit.json`：整包及 120 个输入核验记录。
- `historical_baseline_audit.json`：此前成功配置和结果的关联核验。
- `summary.csv` / `rerun_summary.json`：两个基因的均值、波动和误差。
- `angle_comparison.csv`：12 个角度逐点结果及 FEM/ANS 哈希比较。
- `torque_comparison.png` / `.pdf`：曲线及指标对比。
- `remote_runner.diff`：对方运行器与本机运行器的代码差异。

原 ZIP、正式配置、种子清单及主 FEMM 队列没有修改。本次新增文件均位于本核验目录。
